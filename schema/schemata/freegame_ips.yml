---
"$schema": http://json-schema.org/draft-04/hyper-schema
title: Prizey - freegame ips
description: A free game ips is where user IP blocking is controlled
stability: prototype
type:
- object
definitions:
  ip_blocked:
    description: whether ip is blocked or not
    readOnly: true
    type: boolean
  identity:
    anyOf:
    - "$ref": "/schemata/freegame_ips#/definitions/ip_blocked"
links:
- title: Create
  description: Create free game ip
  href: "/freegame_ips"
  method: POST
  rel: instances
  schema:
    additionalProperties: false
    properties: {}
    type:
    - object
  targetSchema:
    anyOf:
      - "$ref": "/schemata/freegame_ips"
      - "$ref": "/schemata/error"
properties:
  ip_blocked:
    $ref: "/schemata/freegame_ips#/definitions/ip_blocked"
id: schemata/freegame_ips
